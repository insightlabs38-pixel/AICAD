# skills

WP-16 (AI tools + skills). Official AI skill files:
`cad-core.skill.md`, `cad-geometry.skill.md`, `cad-engineering.skill.md`.

An initial core-skill draft is a Stage 0 deliverable (used as a
learnability constraint on the language design itself,
`docs/plan/15_IMPLEMENTATION_ROADMAP.md` Stage 0); the skill set is
finalized in Stage 10.

`cad-core.skill.md` was not produced at Stage 0 (flagged as gap G1 in
`project/gates/stage-0-gate.md`, non-blocking then) and was written by
`AICAD-079` (Stage 3, Batch S3-08) instead, once there was an actual
Stage-3 builtin catalogue/CLI to describe accurately — see that file's
own "Status" line and `project/reports/AICAD-079.md`. It is scoped to
what is implemented today and will need revision as later stages (in
particular Stage 4's semantic references, and any later sketch-syntax
integration) change what is true. `cad-geometry.skill.md`/
`cad-engineering.skill.md` remain unwritten — later-stage scope
(`docs/plan/11_AI_NATIVE_SKILLS_AND_AGENT_PROTOCOL.md` §2's "load only for
advanced work"/"cross-module conventions" tiers, neither reachable yet).

Plan references: `docs/plan/11_AI_NATIVE_SKILLS_AND_AGENT_PROTOCOL.md` §2.
