schema_version: 1
queue_policy: One bounded task at a time. AGENTS.md + CURRENT_STAGE.md govern execution. Owner approves every stage gate.
tasks:
- id: AICAD-001
  stage: 0
  title: Import plan bundle verbatim into docs/plan; record SHA-256/version
  status: todo
  depends_on: []
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-001.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-002
  stage: 0
  title: Create proposed monorepo directory skeleton
  status: todo
  depends_on:
  - AICAD-001
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-002.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-003
  stage: 0
  title: Create Rust workspace/toolchain policy and baseline ignore/editor config
  status: todo
  depends_on:
  - AICAD-002
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-003.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-004
  stage: 0
  title: 'Create baseline CI: fmt, clippy, tests, native build smoke'
  status: todo
  depends_on:
  - AICAD-003
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-004.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-005
  stage: 0
  title: Create OWNER_DECISIONS.md and DECISION_LOG.md from unresolved plan decisions
  status: todo
  depends_on:
  - AICAD-004
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-005.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-006
  stage: 0
  title: Create Stage 0-4 gate-to-test traceability matrix
  status: todo
  depends_on:
  - AICAD-005
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-006.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-007
  stage: 0
  title: Draft RFC-0001 language principles
  status: todo
  depends_on:
  - AICAD-006
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-007.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-008
  stage: 0
  title: Draft RFC-0002 geometry/runtime and kernel abstraction
  status: todo
  depends_on:
  - AICAD-007
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-008.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-009
  stage: 0
  title: Draft RFC-0003 semantic references
  status: todo
  depends_on:
  - AICAD-008
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-009.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-010
  stage: 0
  title: Draft RFC-0004 units/type system
  status: todo
  depends_on:
  - AICAD-009
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-010.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-011
  stage: 0
  title: Draft RFC-0005 diagnostics
  status: todo
  depends_on:
  - AICAD-010
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-011.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-012
  stage: 0
  title: Write Stage-0 paper/spec example spanning the required concepts
  status: todo
  depends_on:
  - AICAD-011
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-012.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-013
  stage: 0
  title: Run contradiction review across RFCs and paper example
  status: todo
  depends_on:
  - AICAD-012
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-013.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-014
  stage: 0
  title: Prepare Stage-0 owner gate packet
  status: todo
  depends_on:
  - AICAD-013
  plan_references:
  - docs/plan/README.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Packet includes RFCs, paper example, unresolved decisions, evidence, and explicit pass/do-not-pass recommendation;
    agent does not self-approve.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-014.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-015
  stage: 1
  title: Add OCCT discovery/probe CMake target
  status: todo
  depends_on:
  - AICAD-014
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-015.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-016
  stage: 1
  title: Create native/occt_bridge C ABI boundary
  status: todo
  depends_on:
  - AICAD-015
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-016.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-017
  stage: 1
  title: Create cad-kernel-api backend-independent handle/error types
  status: todo
  depends_on:
  - AICAD-016
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-017.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-018
  stage: 1
  title: Create cad-occt-bridge safe Rust wrapper
  status: todo
  depends_on:
  - AICAD-017
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-018.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-019
  stage: 1
  title: Implement kernel context lifecycle and shape-handle table
  status: todo
  depends_on:
  - AICAD-018
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-019.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-020
  stage: 1
  title: Implement box and cylinder
  status: todo
  depends_on:
  - AICAD-019
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-020.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-021
  stage: 1
  title: Implement points/vectors/axes/frames and rigid transforms
  status: todo
  depends_on:
  - AICAD-020
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-021.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-022
  stage: 1
  title: Implement line/circle curves and edge/wire creation
  status: todo
  depends_on:
  - AICAD-021
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-022.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-023
  stage: 1
  title: Implement planar face from closed wire
  status: todo
  depends_on:
  - AICAD-022
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-023.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-024
  stage: 1
  title: Implement extrude and revolve
  status: todo
  depends_on:
  - AICAD-023
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-024.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-025
  stage: 1
  title: Implement sweep and loft minimal supported forms
  status: todo
  depends_on:
  - AICAD-024
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-025.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-026
  stage: 1
  title: Implement boolean union/cut/intersect
  status: todo
  depends_on:
  - AICAD-025
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-026.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-027
  stage: 1
  title: Implement fillet and chamfer
  status: todo
  depends_on:
  - AICAD-026
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-027.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-028
  stage: 1
  title: Implement shell/offset spike
  status: todo
  depends_on:
  - AICAD-027
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-028.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-029
  stage: 1
  title: Implement topology exploration
  status: todo
  depends_on:
  - AICAD-028
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-029.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-030
  stage: 1
  title: Implement bounds/length/area/volume/center-of-mass queries
  status: todo
  depends_on:
  - AICAD-029
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-030.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-031
  stage: 1
  title: Implement B-rep validity checks and normalized validation report
  status: todo
  depends_on:
  - AICAD-030
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-031.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-032
  stage: 1
  title: Implement display tessellation output
  status: todo
  depends_on:
  - AICAD-031
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-032.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-033
  stage: 1
  title: Implement STEP export
  status: todo
  depends_on:
  - AICAD-032
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-033.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-034
  stage: 1
  title: Build Stage-1 complex bracket directly through Rust/native API
  status: todo
  depends_on:
  - AICAD-033
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Bracket is a valid exact B-rep; analytic property/topology checks pass; it exports to STEP.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-034.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-035
  stage: 1
  title: Create independent STEP verification harness
  status: todo
  depends_on:
  - AICAD-034
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Exported Stage-1 geometry is independently re-imported/verified and compared by documented non-byte-equality metrics.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-035.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-036
  stage: 1
  title: Add bounded geometry regression/fuzz harness
  status: todo
  depends_on:
  - AICAD-035
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-036.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-037
  stage: 1
  title: Prepare Stage-1 owner gate packet
  status: todo
  depends_on:
  - AICAD-036
  plan_references:
  - docs/plan/01_SYSTEM_ARCHITECTURE.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Packet proves Rust/native -> narrow OCCT bridge -> valid exact bracket -> STEP -> independent verification.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-037.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-038
  stage: 2
  title: Create cad-diagnostics crate and JSON-schema conformance tests
  status: done
  depends_on:
  - AICAD-037
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-038.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-039
  stage: 2
  title: Create cad-ast and cad-lexer with token/span model
  status: done
  depends_on:
  - AICAD-038
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-039.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-040
  stage: 2
  title: Implement numeric literals with engineering-unit suffix tokenization
  status: done
  depends_on:
  - AICAD-039
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-040.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-041
  stage: 2
  title: Implement expression parser and precedence
  status: done
  depends_on:
  - AICAD-040
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-041.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-042
  stage: 2
  title: 'Implement declarations: let/const/param/fn/struct/enum/part'
  status: done
  depends_on:
  - AICAD-041
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-042.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-043
  stage: 2
  title: 'Implement control-flow syntax: if/for/while/match/return'
  status: done
  depends_on:
  - AICAD-042
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-043.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-044
  stage: 2
  title: Implement module/import syntax and loader skeleton
  status: done
  depends_on:
  - AICAD-043
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-044.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-045
  stage: 2
  title: Implement minimal formatter / AST pretty-printer
  status: done
  depends_on:
  - AICAD-044
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-045.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-046
  stage: 2
  title: Create cad-types primitive type representation
  status: done
  depends_on:
  - AICAD-045
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-046.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-047
  stage: 2
  title: Create cad-units dimensional vector/canonicalization per RFC-0004
  status: done
  depends_on:
  - AICAD-046
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-047.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-048
  stage: 2
  title: Implement initial unit registry and conversions
  status: done
  depends_on:
  - AICAD-047
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-048.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-049
  stage: 2
  title: Implement dimensional arithmetic type rules
  status: done
  depends_on:
  - AICAD-048
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Correct derived dimensions infer; illegal mixed-dimension arithmetic fails with stable diagnostics.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-049.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-050
  stage: 2
  title: Implement name binding/scopes/symbol table
  status: done
  depends_on:
  - AICAD-049
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-050.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-051
  stage: 2
  title: Create typed HIR and AST->HIR lowering skeleton
  status: done
  depends_on:
  - AICAD-050
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-051.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-052
  stage: 2
  title: Implement type checking for literals/bindings/functions/calls
  status: done
  depends_on:
  - AICAD-051
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-052.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-053
  stage: 2
  title: Implement structs/enums field and variant typing
  status: done
  depends_on:
  - AICAD-052
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-053.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-054
  stage: 2
  title: Implement function execution and lexical scopes
  status: done
  depends_on:
  - AICAD-053
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-054.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-055
  stage: 2
  title: Implement conditional and match execution
  status: done
  depends_on:
  - AICAD-054
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-055.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-056
  stage: 2
  title: Implement loops and basic collections/iterators
  status: done
  depends_on:
  - AICAD-055
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-056.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057
  stage: 2
  title: Implement recursion and Result/error propagation
  status: done
  depends_on:
  - AICAD-057F
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
  notes: >
    Recursion and call-stack error propagation were implemented in this
    task's first session (see project/reports/AICAD-057.md). Result<T,E>
    construction/matching/propagation was blocked on OWNER_DECISIONS.md#D17
    (DECISION_LOG.md#DL-14) until the AICAD-057A..AICAD-057F remediation
    sequence completed; that sequence is now done (all six status: done).
    This task's own closure session re-verified, with fresh evidence
    against this task's own original acceptance wording, that Result<T,E>
    construction/matching/explicit Err propagation through nested function
    calls via ordinary match is fully covered by the AICAD-057E/057F
    generic-enum prelude machinery, with no new escalation required. See
    project/reports/AICAD-057.md's "Closure (session 2)" section.
- id: AICAD-057A
  stage: 2
  title: Stage-2 coverage audit (generics/enums/Result/Optional/collections/closures/generators/interfaces)
  status: done
  depends_on:
  - AICAD-056
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/21_FEATURE_INVENTORY.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Every capability the owner's D17 ruling names is directly inspected against the current implementation and classified
    REQUIRED-BEFORE-STAGE2-GATE/REQUIRED-LATER/OPTIONAL-FUTURE/ALREADY-SATISFIED; only REQUIRED-BEFORE-STAGE2-GATE gaps
    are added as new remediation tasks; Stage 2 is not silently widened to match the long-term language plan.
  required_checks:
  - run task-specific tests/benchmarks; record exact commands and outputs (planning-only task; no Rust source changed)
  report: project/reports/AICAD-057A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057B
  stage: 2
  title: Implement generic parameter/type-application syntax plus AST/HIR representation
  status: done
  depends_on:
  - AICAD-057A
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - struct/enum/fn declarations can declare an ordinary generic type-parameter list per OWNER_DECISIONS.md#D17/DECISION_LOG.md#DL-14;
    Name<T,U> type application resolves against declared parameters in AST/HIR; no interface/trait bounds, higher-kinded types,
    variance, specialization, variadic generics, dependent types, generic associated types, or lifetime parameters are introduced;
    the design keeps an explicit extension point for a future bound list without replacing the generic type model.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057B.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057C
  stage: 2
  title: Implement data-carrying enum variants, constructors, destructuring patterns, and match exhaustiveness
  status: done
  depends_on:
  - AICAD-057B
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Enums support Unit/Tuple(T1,T2)/Record{x,y} variants per OWNER_DECISIONS.md#D17/DECISION_LOG.md#DL-14; variants are usable as
    constructor expressions; match supports corresponding tuple/record destructuring with normal lexical scope and variant-derived
    binding types; nominal-enum matches missing a variant (without a wildcard/exhaustive pattern) are a diagnostic, not silently
    accepted.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057C.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057D
  stage: 2
  title: Implement generic instantiation/inference/type checking for the approved Stage-2 generic subset
  status: done
  depends_on:
  - AICAD-057C
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Call-site type-parameter instantiation/inference works when type parameters are determinable unambiguously from arguments/expected
    types; an ambiguous generic call produces a diagnostic rather than an arbitrary selection; a wrong number of explicit type
    arguments produces a diagnostic; covers generic structs, generic enums, and generic functions.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057D.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057E
  stage: 2
  title: Define and execute Result<T,E> and Optional<T> using the ordinary generic enum machinery
  status: done
  depends_on:
  - AICAD-057D
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Result<T,E> (Ok(T)/Err(E)) and Optional<T> (Some(T)/None) exist as ordinary prelude generic enums with no Result-specific
    compiler/runtime semantics beyond prelude registration; Result values are propagated with ordinary match, not a new ? operator
    or other propagation syntax; nested generics such as Optional<Result<Int,E>> type-check and execute correctly.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057E.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-057F
  stage: 2
  title: Adversarial integration pass proving generic/enum machinery is general, not Result-specific
  status: done
  depends_on:
  - AICAD-057E
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - A user-defined generic enum whose name is not Result/Optional/List/Range exercises the same generic/pattern/exhaustiveness
    machinery end to end; the full OWNER_DECISIONS.md#D17/DECISION_LOG.md#DL-14 required-test list passes; no ? syntax and no
    general compiler-intrinsic facility were added anywhere in AICAD-057B..057F.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-057F.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-058
  stage: 2
  title: Implement execution resource-budget accounting
  status: done
  depends_on:
  - AICAD-057
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-058.md
  notes: >
    Unified AICAD-056's for-loop iteration budget and AICAD-057's
    recursion-depth budget into one Interpreter::ResourceBudget, closing a
    real gap along the way (while/loop had no iteration bound at all
    before this task); added Interpreter::resource_usage() as the
    "accounting" half; moved IterationBudgetExceeded/RecursionLimitExceeded
    from RUNTIME-E123/E124 to the dedicated BUDGET-E001/E002 family
    docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md already reserves for this.
    max_cpu_time/max_memory/max_geometry_ops/max_faces/max_solids
    deliberately deferred (no wall-clock/allocation hook or Geometry IR
    exists yet to measure them against) - see project/reports/AICAD-058.md
    decision 3.
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-059
  stage: 2
  title: Create backend-independent Geometry IR
  status: done
  depends_on:
  - AICAD-058
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-059.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-060
  stage: 2
  title: Implement HIR/runtime geometry dispatch into Geometry IR/kernel API
  status: done
  depends_on:
  - AICAD-059
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-060.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-061
  stage: 2
  title: Create cad-cli build command with human and JSON diagnostics
  status: done
  depends_on:
  - AICAD-060
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-061.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-062
  stage: 2
  title: Create Tree-sitter grammar for supported syntax subset
  status: done
  depends_on:
  - AICAD-061
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-062.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-063
  stage: 2
  title: Implement end-to-end source -> exact bracket -> STEP slice
  status: done
  depends_on:
  - AICAD-062
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - AICAD source using typed units and ordinary control flow compiles through HIR/Geometry IR to valid exact bracket
    and verified STEP.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-063.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-064
  stage: 2
  title: Prepare Stage-2 owner gate packet
  status: done
  depends_on:
  - AICAD-063
  plan_references:
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/17_CLI_DIAGNOSTICS_SCHEMA.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Packet proves ordinary programs construct parameterized geometry without special interpreter shortcuts.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-064.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-064A
  stage: 3
  title: Implement/calibrate the D5 v1 cad-validation comparison profile
  status: done
  depends_on:
  - AICAD-064
  plan_references:
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - The comparison-profile module exists in crates/cad-validation, preserving D5's dimensional/scale-aware
    shape (DECISION_LOG.md#DL-12).
  - A bounded multi-scale calibration corpus (approximately 1mm/10mm/100mm/1000mm; primitives, transforms,
    booleans, fillets/chamfers, analytically checkable area, analytically checkable volume, center of mass,
    repeated rebuilds) runs and its measured absolute error, relative error, and repeat-run drift are recorded
    separately in the task report.
  - The three owner-accepted v1 constants from DECISION_LOG.md#DL-17 (linear_abs, center_of_mass_abs,
    volume_rel) are implemented; any remaining constant the calibration evidence does not clearly justify is
    escalated to project/OWNER_DECISIONS.md rather than guessed.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-064A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
  - calibration evidence does not clearly justify a specific constant (escalate rather than guess)
- id: AICAD-065
  stage: 3
  title: Implement first-class param declarations and derived expressions
  status: done
  depends_on:
  - AICAD-064A
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-065.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-066
  stage: 3
  title: Create cad-feature-graph node identity/dependency model
  status: done
  depends_on:
  - AICAD-065
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-066.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-067
  stage: 3
  title: Build feature DAG from supported modeling operations
  status: done
  depends_on:
  - AICAD-066
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-067.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-068
  stage: 3
  title: Implement cache keys and dirty propagation
  status: done
  depends_on:
  - AICAD-067
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-068.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-069
  stage: 3
  title: Implement source-to-feature mapping and provenance
  status: done
  depends_on:
  - AICAD-068
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-069.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-070
  stage: 3
  title: Create safe language-facing geometry types
  status: done
  depends_on:
  - AICAD-069
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-070.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-071
  stage: 3
  title: Implement part concept plus box/cylinder/plate standard helpers
  status: done
  depends_on:
  - AICAD-070
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-071.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-072
  stage: 3
  title: Create minimal sketch entity IR
  status: done
  depends_on:
  - AICAD-071
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-072.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-073
  stage: 3
  title: Create solver-independent sketch constraint IR/adapter
  status: done
  depends_on:
  - AICAD-072
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-073.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-074
  stage: 3
  title: Implement common sketch constraints needed by baseline parts
  status: done
  depends_on:
  - AICAD-073
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-074.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-075
  stage: 3
  title: Lower solved closed sketch profiles to exact faces
  status: done
  depends_on:
  - AICAD-074
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-075.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-075A
  stage: 3
  title: Establish general axis/frame/rotation semantics needed by revolve, mirror, circular pattern, and non-translation transforms
  status: done
  depends_on:
  - AICAD-075
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - One coherent axis/frame/rotation representation serves transform, revolve, mirror, and circular pattern —
    no feature invents its own coordinate convention.
  - Existing RFC/plan semantics and the Stage-1 kernel-api Frame3/Axis3/Transform primitives
    (crates/cad-kernel-api/src/geometry.rs) are audited first and reused rather than duplicated.
  - No larger spatial-math system than Stage 3 needs is introduced.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-075A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
  - exact public syntax/semantics remain materially ambiguous after reading the approved plan/RFCs
- id: AICAD-076
  stage: 3
  title: Implement high-level extrude/revolve/hole/pocket
  status: done
  depends_on:
  - AICAD-075A
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-076.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-076A
  stage: 3
  title: Make the RuntimeBuiltin catalogue's standard type environment type-closed
  status: done
  depends_on:
  - AICAD-076
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  - project/DECISION_LOG.md#DL-21
  acceptance:
  - Every nominal type referenced by any BuiltinFnId signature (including Point3/Axis3/Frame3/Plane) is resolvable
    without the compiling program separately composing cad_hir::geometry_types::with_geometry_types.
  - A dedicated test proves the entire BuiltinFnId catalogue type-checks against an otherwise-empty valid program
    with zero diagnostics (project/DECISION_LOG.md#DL-21's own required invariant), separate from any test of an
    individual builtin's own expected signature.
  - with_geometry_types remains safe to call on top of the now-always-seeded standard types (idempotent; no
    duplicate/conflicting declaration).
  - AICAD-076's scalar-decomposed revolve/hole/pocket signatures are migrated to real Axis3/Frame3-typed parameters
    where AICAD-075A already defines the needed type; the "Why no Axis3/Frame3-typed parameter yet" note in
    crates/cad-hir/src/builtins.rs is removed/updated accordingly.
  - The existing eager function-signature-collection model is preserved unchanged (no lazy per-call-site resolution).
  - No plugin-injected/runtime-registered builtin types, arbitrary host callbacks, or kernel/OCCT types in
    source/HIR signatures are introduced.
  - cargo test -p cad-hir and the full workspace suite pass with zero failures (the 149-test regression AICAD-076
    found during development must not recur).
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-hir
  - cargo test --workspace
  - cargo test -p cad-cli --test stage2_end_to_end -- --test-threads=1
  report: project/reports/AICAD-076A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected (beyond what project/DECISION_LOG.md#DL-21 already
    settled)
  - work would expand into a later roadmap stage
  - lazy per-call-site signature resolution would be required to proceed (DL-21 explicitly does not authorize this)
- id: AICAD-077
  stage: 3
  title: Implement mirror and linear/circular pattern basics
  status: done
  depends_on:
  - AICAD-076A
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-077.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-078
  stage: 3
  title: Implement high-level fillet/chamfer/shell wrappers and normalized diagnostics
  status: done
  depends_on:
  - AICAD-077
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-078.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-079
  stage: 3
  title: Implement named semantic outputs baseline + ordinary-part examples + AI benchmark seed
  status: done
  depends_on:
  - AICAD-078
  plan_references:
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Human source can expose semantic names; baseline ordinary parts build; fresh-model simple-part benchmark can run
    using the core-skill draft.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-079.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-079A
  stage: 3
  title: Freeze the Stage-4 semantic-reference benchmark and held-out corpus; minimal docs/archive scaffolding
  status: done
  depends_on:
  - AICAD-079
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  acceptance:
  - A curated Stage-4 semantic-reference benchmark/held-out corpus is frozen, covering at minimum topology
    splits/merges, disappearing/newly-created entities, symmetric candidates, pattern-count changes, boolean
    topology changes, fillet/chamfer viability changes, valid operation reordering, upstream suppression,
    changing sketch regions, and near-degenerate but valid geometry.
  - Expected-result classification distinguishes correct resolved reference, explicit ambiguity, explicit
    broken reference, silent wrong selection, kernel failure, and unrelated build failure.
  - A held-out corpus/process is established and is not routine Stage-4 tuning material.
  - This task implements no Stage-4 semantic-reference resolution itself.
  - Minimal docs/archive directory scaffolding exists only where git requires a placeholder (docs/site/user/,
    docs/site/language/, docs/site/modeling/, docs/site/developer/, project/reports/archive/stage{0,1,2,3}/),
    with no MkDocs config, tutorials, README rewrite, or report migration performed.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-079A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
  - any Stage-4 semantic-reference resolution logic would need to be implemented to build the benchmark
- id: AICAD-079B
  stage: 3
  title: Prepare the Stage-3 owner gate packet
  status: done
  depends_on:
  - AICAD-079A
  plan_references:
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  acceptance:
  - No new roadmap feature development occurs in this task; it audits current code/tests, not merely
    previous report claims, and re-runs the complete required workspace suite.
  - The gate packet proves the parametric-build slice and the sketch/high-level modeling slice end to end,
    per the campaign brief's exact acceptance list (D5 determinism, kernel neutrality, no persistent
    raw-topology identity, source/provenance traceability, no leaked Stage-4 semantic-reference
    implementation, no weakened tests/gates, ordinary-part examples use ordinary public paths, Stage-4
    benchmark frozen before Stage-4 work, unresolved owner decisions enumerated).
  - Exactly one recommendation (PASS / PASS WITH CONDITIONS / DO NOT PASS) is produced; the recommendation
    is advisory only and does not itself approve Stage 3.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-079B.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-079C
  stage: transition
  title: Stage-4 CI/CD infrastructure expansion and transition finalization
  status: done
  depends_on:
  - AICAD-079B
  plan_references:
  - project/gates/stage-3-gate.md
  - project/planning/transitions/stage3-to-stage4/README.md
  - project/benchmarks/stage4_semantic_reference/README.md
  - docs/developer/testing/README.md
  acceptance:
  - Stage-4 layered CI covers fast required checks, exact-geometry integration, AICAD-owned determinism, semantic-reference
    corpus/harness plumbing, and appropriately scheduled/manual hardening without implementing semantic-reference behavior.
  - The AICAD-079A frozen corpus is consumable by the resolver-independent Stage-4 harness, with explicit
    RESOLVED_CORRECT/AMBIGUOUS/BROKEN/SILENT_WRONG/KERNEL_FAILURE/UNRELATED_FAILURE outcome classes and a permanent
    minimized-regression policy for every discovered SILENT_WRONG result.
  - Platform, performance, dependency/supply-chain, release-build, failure-artifact, and branch-protection foundations are
    documented and bounded to currently supported/evidenced capability.
  - Stage-3 -> Stage-4 governance records the transition complete pending owner review/merge and Stage 4 ready but not yet
    implemented; AICAD-080 remains todo and no Stage-5/AICAD-101+ work is promoted.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - native OCCT bridge CMake build plus CTest
  - python scripts/ci/semantic_ref_harness.py validate
  - python scripts/ci/semantic_ref_harness.py self-test
  - python scripts/ci/stage4_task_audit.py --check
  report: project/reports/AICAD-079C.md
  escalate_if:
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - any production Stage-4 semantic-reference implementation would be required to complete this infrastructure task
  - work would expand into Stage 5 or AICAD-101+
- id: AICAD-080
  stage: 4
  title: Define stable VertexRef/EdgeRef/WireRef/FaceRef/ShellRef/SolidRef semantic-recipe representations
  status: done
  depends_on:
  - AICAD-079C
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  - project/benchmarks/stage4_semantic_reference/README.md
  - tests/semantic_refs/README.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-080.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-081
  stage: 4
  title: Create query AST/IR with cardinality expectations and deterministic ranking
  status: done
  depends_on:
  - AICAD-080
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-081.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-082
  stage: 4
  title: 'Implement geometry predicates: planar/cylindrical/radius/area/length/normal/axis'
  status: done
  depends_on:
  - AICAD-081
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-082.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-083
  stage: 4
  title: 'Implement topology predicates: generated_by/modified_by/descended_from/adjacent_to/boundary'
  status: done
  depends_on:
  - AICAD-082
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-083.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-084
  stage: 4
  title: Implement baseline spatial predicates
  status: done
  depends_on:
  - AICAD-083
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-084.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-085
  stage: 4
  title: Implement explicit semantic feature exports
  status: done
  depends_on:
  - AICAD-084
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-085.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-086
  stage: 4
  title: Capture available generated/modified lineage from kernel operations
  status: done
  depends_on:
  - AICAD-085
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-086.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-087
  stage: 4
  title: Store feature-level lineage for unchanged/new/modified/split/merged/deleted entities
  status: done
  depends_on:
  - AICAD-086
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-087.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-088
  stage: 4
  title: Implement semantic-reference resolver using approved precedence
  status: done
  depends_on:
  - AICAD-087
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-088.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-089
  stage: 4
  title: Implement ambiguity-as-error diagnostic with candidate summaries
  status: done
  depends_on:
  - AICAD-088
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - A single-reference query resolving multiple candidates never silently chooses an entity.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-089.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-090
  stage: 4
  title: Implement broken-reference diagnostic and non-guessing recovery hints
  status: done
  depends_on:
  - AICAD-089
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-090.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-091
  stage: 4
  title: Implement reference durability levels
  status: done
  depends_on:
  - AICAD-090
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-091.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-092
  stage: 4
  title: Implement geometry-fingerprint evidence/ranking/benchmark support without automatic recovery
  status: done
  depends_on:
  - AICAD-091
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  - project/DECISION_LOG.md#DL-8
  - tests/semantic_refs/README.md
  acceptance:
  - Fingerprint information is available only as diagnostic evidence, candidate-ranking input, and benchmark/experiment data;
    it never converts Ambiguous/Broken into Resolved automatically in the first Stage-4 implementation.
  - Any proposal to enable automatic fingerprint recovery requires a later explicit owner decision supported by Stage-4
    benchmark evidence; D7/DL-8 remains authoritative.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-092.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-093
  stage: 4
  title: Implement raw topology handle epochs/stale-handle rejection
  status: done
  depends_on:
  - AICAD-092
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-093.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-094
  stage: 4
  title: Replay semantic references during incremental parameter regeneration
  status: done
  depends_on:
  - AICAD-093
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-094.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-095
  stage: 4
  title: Create cad refs check / reference-health report
  status: done
  depends_on:
  - AICAD-094
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-095.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-096
  stage: 4
  title: Extend the frozen AICAD-079A topology-reference corpus for resolver execution
  status: done
  depends_on:
  - AICAD-095
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  - project/benchmarks/stage4_semantic_reference/README.md
  - tests/semantic_refs/README.md
  acceptance:
  - The frozen AICAD-079A public/held-out corpus remains the baseline; Stage-4 resolver execution extends coverage without
    rewriting held-out ground truth or recreating the baseline corpus.
  - Coverage includes extrusion resize, add/remove hole, pattern count, fillet radius, split, merge, branch reorder, and
    suppression perturbations, reusing the AICAD-079A harness/outcome taxonomy where applicable.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-096.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-097
  stage: 4
  title: Create deterministic perturbation runner
  status: done
  depends_on:
  - AICAD-096
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Focused implementation plus task-specific tests/benchmarks satisfy the referenced plan requirements; exact commands/results
    are recorded in the task report.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-097.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-098
  stage: 4
  title: 'Implement benchmark metrics: correct / ambiguous-detected / broken-detected / silent-wrong / durability'
  status: done
  depends_on:
  - AICAD-097
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Metrics are deterministic/reproducible and each silent-wrong result is directly inspectable.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-098.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-099
  stage: 4
  title: Run adversarial bug-hunt; minimize and preserve every silent-wrong reproducer
  status: done
  depends_on:
  - AICAD-098
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Every discovered silent-wrong case becomes a minimized permanent regression fixture; expected behavior is never weakened
    to make it pass.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-099.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
- id: AICAD-099A
  stage: 4
  title: Scoped candidate-universe resolution (AICAD-099 remediation)
  status: done
  depends_on:
  - AICAD-099
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  acceptance:
  - A semantic-reference query/resolution request has an explicit, AICAD-owned way (FeatureAnchor-keyed, not a topology
    index/OCCT handle/fingerprint) to scope its candidate universe to one existing named feature/binding before predicate/
    ranking/cardinality evaluation.
  - A missing/invalid requested scope fails explicitly (Broken) and never silently falls back to the unscoped whole-candidate-
    universe search.
  - Same-geometry candidates within a resolved scope are never deduplicated.
  - The unscoped production API keeps its pre-existing whole-live-universe semantics, still covered by a passing test.
  - case03's own acceptance test resolves through the real production path (ParametricBuildSession::resolve / crate::
    perturbation::run_case), not a test-only SingleShapeContext stand-in, proving baseline Resolved(correct target) and
    perturbed Ambiguous once scoped.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - python3 scripts/ci/semantic_ref_harness.py validate
  - python3 scripts/ci/semantic_ref_harness.py self-test
  - python3 scripts/ci/stage4_task_audit.py --check
  report: project/reports/AICAD-099A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage
  - the fix would require a new unresolved public semantic decision rather than an internal use of existing binding/feature
    identity
- id: AICAD-100
  stage: 4
  title: Prepare Stage-4 hard-gate packet for owner review
  status: done
  depends_on:
  - AICAD-099A
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/15_IMPLEMENTATION_ROADMAP.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - docs/plan/22_REPOSITORY_WORK_PACKAGES.md
  acceptance:
  - Packet reports full topology-naming results including silent-wrong count and known limits; agent must not start broad
    Stage-5 feature expansion until owner passes the gate.
  required_checks:
  - cargo fmt --all -- --check (when Rust workspace exists)
  - cargo clippy --workspace --all-targets --all-features -- -D warnings (when applicable)
  - run task-specific tests/benchmarks; record exact commands and outputs
  report: project/reports/AICAD-100.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage

- id: AICAD-100A
  stage: 4
  title: Complete production semantic-reference integration disclosed by the Stage-4 gate packet
  status: done
  depends_on:
  - AICAD-100
  plan_references:
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md
  - rfcs/0003-semantic-references.md
  - specs/language/grammar.ebnf
  - project/OWNER_DECISIONS.md#D31
  acceptance:
  - 'D31 resolved by explicit owner ruling ("part { ... } is an abstraction/scope boundary, not a feature-visibility
    barrier") and cad_feature_graph::FeatureGraph discovers geometry-producing features inside part bodies with
    AICAD-owned scoped identity/provenance, never OCCT identity.'
  - Persistent-reference candidate scope is explicit in production -- whole-session unscoped resolution is no longer the
    implicit default for a query/reference, geometrically-distinct candidates are never deduplicated, and an invalid or
    missing required scope fails closed.
  - All Stage-4 predicate variants (Convex/Concave/Manifold/NonManifold/ConnectedTo/Contains/Intersects plus ancestry/
    adjacent_to/inside/within) have real, kernel-neutral production semantics with positive/negative/invalid-kind test
    coverage.
  - Every ConstructionStrategy (ExplicitExport/FeatureLineage/SemanticQuery/StructuralRole/Ancestry/UserConfirmed) has a
    real production evidence source backed by at least one real ParametricBuildSession test; GeometricFingerprint remains
    D7-fail-closed (Broken), never an automatic resolution basis.
  - Face and Edge lineage coverage is complete across unchanged/generated/modified/split/merged/deleted states; Shell/
    Solid candidate enumeration is wired through kernel-neutral bridge accessors.
  - RFC-0003 section 7's reserved query { ... } surface is promoted into specs/language/grammar.ebnf as the smallest
    syntax completion consistent with that surface and existing AICAD binding/value semantics; a source-declared
    persistent reference lowers into real cad_query::Query/cad_references::AnyRef values (never a test-only injection)
    and cad refs check observes a non-empty, really-resolved reference set from a real .aicad program.
  - The frozen AICAD-079A corpus's own official intended queries are executed directly through the real production path
    where the underlying operation actually supplies lineage evidence, never synthesized from geometric coincidence, with
    expected targets left unmodified to fit implementation results; the silent-wrong count is zero.
  - A limitation sweep across cad-references/cad-query/cad-feature-graph/reference replay/ParametricBuildSession/
    perturbation runner/cad refs check/HIR and runtime reference lowering/kernel topology enumeration found no remaining
    hidden Stage-4 gap; the one real completeness gap found (part-in-part nesting silently inert) is a pre-existing,
    non-Stage-4-specific limitation, explicitly recorded rather than buried.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - python scripts/ci/semantic_ref_harness.py validate
  - python scripts/ci/semantic_ref_harness.py self-test
  report: project/reports/AICAD-100A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types would leak above the adapter
  - semantic-reference ambiguity would require arbitrary fallback
  - an unresolved architecture alternative must be selected
  - work would expand into a later roadmap stage (Stage 5/AICAD-101+)
- id: AICAD-101
  stage: 5
  title: Make nested part semantics explicit and non-silent
  status: done
  depends_on:
  - AICAD-100A
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/gates/stage-4-gate.md
  - project/reports/AICAD-100A.md
  - specs/language/grammar.ebnf
  - project/OWNER_DECISIONS.md#D31
  - project/DECISION_LOG.md#DL-33
  acceptance:
  - Every grammatically accepted nested part is either executed recursively with deterministic AICAD-owned scope paths across
    interpreter, feature graph, binding walkers, and query lowering, or is rejected by a stable diagnostic before execution;
    silently inert nested parts are impossible.
  - If nested parts are supported, qualified feature/reference identity extends to arbitrary supported nesting depth without
    using OCCT/native identity, and bare-name collisions still fail closed.
  - Feature/provenance discovery and source-declared query lowering observe the same nesting semantics; no call site retains
    a one-level-only special case.
  - Focused source-level tests cover at least two nested levels, repeated leaf names in different scopes, and the chosen unsupported-depth/error
    case if any bound remains.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-parser -p cad-hir -p cad-runtime -p cad-feature-graph -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-101.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - supporting nested parts would require a new public semantic choice not already implied by D31
- id: AICAD-102
  stage: 5
  title: Complete dimensional and spatial source-value construction required by Stage-5 queries
  status: done
  depends_on:
  - AICAD-100A
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/reports/AICAD-100A.md
  - rfcs/0004-units-type-system.md
  - docs/plan/03_TYPE_SYSTEM_UNITS_CONTROL_FLOW.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  acceptance:
  - Source programs can construct the typed dimensional/spatial values required by the existing query model, including an
    Area-dimensioned quantity path and the Point3/Vector3/Direction/Axis3/Frame3 values needed by Stage-5 query arguments.
  - Area and spatial values preserve typed-unit and frame semantics through AST/HIR/runtime lowering; no bare-f64 or display-unit-dependent
    shortcut is introduced.
  - The always-seeded standard type environment remains type-closed and deterministic; construction uses ordinary language/library/runtime
    mechanisms rather than a geometry-specific compiler intrinsic.
  - Positive, type-error, dimension-mismatch, and round-trip/source-to-runtime tests cover every newly source-constructible
    value family.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-units -p cad-types -p cad-hir -p cad-runtime -p cad-query -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-102.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - the Area/spatial source form requires semantics that RFC-0004 and existing spatial rulings do not determine
- id: AICAD-103
  stage: 5
  title: Complete .aicad lowering for the remaining Stage-4 query vocabulary
  status: done
  depends_on:
  - AICAD-101
  - AICAD-102
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/gates/stage-4-gate.md
  - project/reports/AICAD-100A.md
  - rfcs/0003-semantic-references.md
  - specs/language/grammar.ebnf
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - project/DECISION_LOG.md#DL-8
  acceptance:
  - Every Stage-4 predicate/ranking operation that already has production Rust semantics and is required by the approved query
    surface has a real .aicad source form and lowers into the same cad_query/cad_references values; no test-only injection
    path is needed.
  - Nested reference/query arguments and Point3/Frame3-dependent predicates are represented explicitly and type checked; malformed
    or unsupported forms produce stable diagnostics rather than being ignored.
  - Cardinality, scope, durability, and ranking semantics remain fail-closed and identical to the Stage-4 resolver contract;
    this task does not enable automatic fingerprint recovery.
  - A source-vocabulary conformance table/test proves source spelling -> HIR/lowering -> production predicate coverage and
    flags any intentionally deferred Rust-level predicate explicitly.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-lexer -p cad-parser -p cad-hir -p cad-query -p cad-references -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-103.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - completing the source form would require changing Stage-4 resolver semantics rather than only exposing already-approved
    semantics
- id: AICAD-104
  stage: 5
  title: Prove the complete source query vocabulary through the production path
  status: done
  depends_on:
  - AICAD-103
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/gates/stage-4-gate.md
  - project/reports/AICAD-100A.md
  - project/benchmarks/stage4_semantic_reference/README.md
  - tests/semantic_refs/README.md
  - project/DECISION_LOG.md#DL-8
  acceptance:
  - Representative .aicad programs exercise every source-visible query family through the real parser/HIR/runtime/ParametricBuildSession/resolver
    path, not direct Rust construction.
  - Positive, no-match, ambiguous, invalid-scope, wrong-cardinality, nested-reference, and spatial-value cases prove Resolved/Ambiguous/Broken
    behavior without arbitrary selection.
  - The frozen Stage-4 corpus remains unchanged and the official semantic-reference harness still validates with zero silent-wrong
    regressions; fingerprint automatic recovery remains disabled.
  - Any source-visible query limitation remaining after this task is named explicitly in the report rather than hidden behind
    a lower-level API.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-cli --test stage4_resolver_execution -- --test-threads=1
  - python3 scripts/ci/semantic_ref_harness.py validate
  - python3 scripts/ci/semantic_ref_harness.py self-test
  - cargo test --workspace
  report: project/reports/AICAD-104.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-104A
  stage: 5
  title: Integrate part-body parameters into ParamModel
  status: done
  depends_on:
  - AICAD-101
  plan_references:
  - project/reports/AICAD-101.md
  - crates/cad-runtime/src/params.rs
  - crates/cad-feature-graph/src/graph.rs
  - project/OWNER_DECISIONS.md#D31
  - project/DECISION_LOG.md#DL-33
  - project/DECISION_LOG.md#DL-36
  acceptance:
  - Every supported part-body param (single-level and nested, matching AICAD-101's own unbounded-depth part semantics) is
    represented by ParamModel with deterministic lexical/scoped identity (the same D31 Vec<String> scope-path convention as
    FeatureNode::scope/qualified_feature_name), never flattened to a bare name.
  - Two different parts declaring a param with the same leaf name never collide in ParamModel's own identity/dependency graph.
  - Existing typed/unit/derived-expression param semantics (default expressions, override type checking, dependency-edge
    computation) are unchanged and apply identically to a part-scoped param.
  - A part-scoped param participates in ParamModel's dependency graph exactly like a top-level one, including transitive
    dependency edges into/out of ordinary top-level params.
  - Editing a part-scoped param's override triggers correct FeatureGraph dirty propagation and incremental rebuild: only
    the affected dependent geometry recomputes; unaffected results are reused, through the real ParamModel -> FeatureGraph
    -> ParametricBuildSession production path (never a test-only model).
  - Relevant provenance for a part-scoped param is retained (no loss of scope/identity information through the parameter
    pipeline).
  - No new source syntax; no change to existing (non-part-scoped) parameter semantics; part-local parameter identity is
    represented with an explicit scope path, never globally flattened.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-runtime -p cad-feature-graph -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-104A.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - part-local parameter identity would need to be globally flattened to fit the existing ParamModel/FeatureGraph API shape
- id: AICAD-105
  stage: 5
  title: Scale the closed RuntimeBuiltin catalogue and implement tracked kernel-backed query evaluation
  status: done
  depends_on:
  - AICAD-104
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-23
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-15
  - project/DECISION_LOG.md#DL-21
  - docs/API/safe-cad-api.md
  - docs/plan/02_LANGUAGE_AND_COMPILER.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  acceptance:
  - The first-party RuntimeBuiltin catalogue has one deterministic authoritative declaration source for identity, signatures,
    required standard types, category/effect metadata, runtime dispatch, and conformance metadata where practical; stable
    builtin identity does not depend accidentally on source ordering.
  - The catalogue remains closed and type-closed with eager signature validation; no plugin/native registration ABI, arbitrary
    host callback, compiler geometry intrinsic, or OCCT type enters source/HIR signatures.
  - Kernel-backed query calls execute as ordinary typed RuntimeBuiltin calls using D23 demand materialization, with dependency
    tracking, determinism, resource accounting, diagnostics, and cache invalidation/provenance hooks sufficient for later
    Stage-5 query families.
  - A catalogue-wide conformance test proves every declaration has exactly one typed runtime implementation path, and source-control-flow
    tests prove query results can drive ordinary program evaluation without bypassing Geometry IR/kernel-neutral boundaries.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-hir -p cad-runtime -p cad-geometry-runtime -p cad-query -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-105.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - catalogue scaling appears to require open runtime registration or a compiler-intrinsic geometry path
  - query realization cannot be made dependency/resource-visible under D23
- id: AICAD-106
  stage: 5
  title: Establish typed Stage-5 modeling/construction and approximation tolerance primitives
  status: done
  depends_on:
  - AICAD-104
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-12
  - rfcs/0004-units-type-system.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  acceptance:
  - Stage-5 APIs can carry explicit typed modeling/construction and approximation tolerance policy without reusing the D5
    equivalence profile, sketch solver tolerance, or a private validity epsilon as one global value.
  - Policy values have dimensional semantics, ownership, deterministic defaults only where evidence/specification supports
    them, bounded explicit overrides, and inspectable provenance/evidence where behavior changes materially.
  - No tolerance is silently widened after a failed operation or test; invalid dimensions/ranges/unsupported overrides fail
    with stable diagnostics.
  - Tests prove domain separation, scale/unit invariance, deterministic serialization/equality where policy values enter AICAD-owned
    state, and rejection of accidental cross-domain reuse.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-units -p cad-validation -p cad-geometry-api -p cad-query
  - cargo test --workspace
  report: project/reports/AICAD-106.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - a numeric default would have to be guessed without evidence
  - one tolerance domain would have to inherit another domain's values implicitly
- id: AICAD-107
  stage: 5
  title: Preserve feature identity, dependencies, and provenance through ordinary language abstractions
  status: done
  depends_on:
  - AICAD-105
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-27
  - docs/plan/04_HIGH_LEVEL_MODELING_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - project/reports/AICAD-100A.md
  acceptance:
  - Geometry-producing/effectful operations remain represented in the feature/dependency/provenance system when executed through
    user functions, reusable helpers/parts, supported branches/loops, repeated calls, and first-party library abstractions.
  - Call-instance identity is deterministic and AICAD-owned; repeated calls and same-named helpers do not collapse distinct
    semantic features, and abstraction never falls back to OCCT/native identity.
  - Parameter dependencies and dirty propagation cross abstraction boundaries so a localized edit invalidates the correct
    dynamic feature instances without rebuilding unrelated work.
  - Source-to-feature navigation and Stage-4 semantic-reference evidence remain available through abstraction, with adversarial
    tests for nested helpers, branches, loops/repeated calls, and name collisions.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-feature-graph -p cad-runtime -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-107.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - a chosen graph encoding would narrow D25's required behavior or erase identity/provenance in a supported abstraction
- id: AICAD-108
  stage: 5
  title: Add kernel-neutral advanced geometry value, result, report, and execution-IR families
  status: done
  depends_on:
  - AICAD-105
  - AICAD-106
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  - rfcs/0002-geometry-runtime-kernel-abstraction.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  acceptance:
  - Kernel-neutral AICAD value/IR families exist for the Stage-5 curve, surface, multi-result query, topology, raw-handle,
    adoption, and operation-report concepts needed by later tasks without exposing OCCT classes or native pointers.
  - Safe semantic values, persistent references, raw/unsafe handles, and kernel-native objects remain distinct types/identity
    domains; serialization/caching never persists a native handle as semantic identity.
  - Multi-solution result structures can represent zero/one/many results plus ordered deterministic evidence/status without
    forcing an arbitrary single result.
  - Architecture/dependency tests keep native/kernel-specific code below the adapter and prove public/HIR/Geometry IR representations
    are deterministic and inspectable.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-types -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api
  - cargo test --workspace
  report: project/reports/AICAD-108.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - a required Stage-5 value can only be represented by leaking a kernel-native object or raw pointer
- id: AICAD-109
  stage: 5
  title: Implement analytic curve families end to end
  status: done
  depends_on:
  - AICAD-108
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - rfcs/0002-geometry-runtime-kernel-abstraction.md
  - project/DECISION_LOG.md#DL-2
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - The approved analytic curve families needed by Stage 5 are constructible/evaluable as safe kernel-neutral values through
    ordinary source/runtime calls and the kernel adapter, with explicit parameter domains and typed spatial inputs.
  - Evaluation and derivative/tangent results use deterministic typed values and report invalid/degenerate inputs structurally;
    no native curve object escapes the adapter.
  - Exact/analytic fixtures verify endpoints, continuity/domain behavior, transformed curves, and independently checkable
    geometry rather than relying on renders.
  - Topology or provenance produced from curves carries the semantic operation/source context needed by later lineage/reference
    tasks.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-109.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-110
  stage: 5
  title: Implement Bezier, B-spline, and NURBS curve families
  status: done
  depends_on:
  - AICAD-109
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Bezier/B-spline/NURBS curves support the bounded Stage-5 construction/evaluation surface with explicit degree, knot/multiplicity,
    control-point, weight, parameter-domain, and validation semantics.
  - Invalid knot vectors, multiplicities, degree/control-point relationships, non-finite data, invalid weights, and degenerate
    inputs fail deterministically with structured diagnostics/reports.
  - Tests include analytically known low-degree cases, endpoint/interpolation invariants where applicable, transformed/trimmed
    parameter domains, and independent sampled/reference checks within the approved approximation policy.
  - The public representation remains kernel-neutral and does not serialize or expose OCCT-native curve identity.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-110.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-111
  stage: 5
  title: Implement curve operations with explicit result and tolerance semantics
  status: done
  depends_on:
  - AICAD-109
  - AICAD-110
  - AICAD-106
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Stage-5 curve-local operations include the approved bounded set of trimming, interpolation/fitting where specified, offset,
    evaluation/derivative, closest/projection/distance helpers needed by later surfaces/queries, each with typed inputs and
    explicit tolerance ownership.
  - Operations that can yield multiple or no mathematical solutions return explicit deterministic result collections/status;
    they never choose an arbitrary candidate to satisfy a single-result convenience API.
  - Approximation/fitting operations report achieved error/evidence against the approximation tolerance rather than silently
    widening it.
  - Regression/property fixtures cover endpoint/domain preservation, transformed inputs, near-degenerate cases, zero/multiple-result
    cases, and deterministic repeated execution.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge
  - cargo test --workspace
  report: project/reports/AICAD-111.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-112
  stage: 5
  title: 'Checkpoint A: curves, runtime/query foundations, abstraction provenance, and examples'
  status: done
  depends_on:
  - AICAD-107
  - AICAD-111
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-23
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-27
  acceptance:
  - A source-level vertical slice constructs analytic and freeform curves, evaluates/operates on them through the real RuntimeBuiltin/kernel-neutral
    path, and verifies exact/analytic properties with no OCCT leakage or compiler-intrinsic shortcut.
  - 'D21-D25 invariants are re-audited against actual implementation: closed catalogue, tracked query realization, separated
    tolerance domains, and feature/provenance visibility through abstraction all have direct tests.'
  - At least one ACTIVE teaching example and one realistic example for the new curve capability execute in the automated example
    suite/CI; no stale example is presented as current supported usage.
  - The checkpoint report records full-workspace results, known limitations, and any architecture conflict; Stage-5 surface
    work does not continue past a failed checkpoint.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - native OCCT bridge CMake build plus CTest
  - run the automated ACTIVE example suite and record exact commands/results
  report: project/reports/AICAD-112.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - Checkpoint A evidence contradicts any D21-D25 invariant
- id: AICAD-113
  stage: 5
  title: Implement analytic surface families end to end
  status: done
  depends_on:
  - AICAD-112
  - AICAD-108
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - The approved analytic surface families needed by Stage 5 are constructible/evaluable through kernel-neutral safe values
    with explicit parameter domains, orientation/frame semantics, and typed source/runtime calls.
  - Point/normal/derivative evaluation is deterministic and dimensionally correct; singular/degenerate parameter locations
    are reported structurally.
  - Independent analytic fixtures verify geometry/properties/domain behavior and transformations without render-only evidence.
  - No native surface identity escapes the adapter and any produced topology retains operation/source context for later lineage.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-113.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-114
  stage: 5
  title: Implement Bezier, B-spline, and NURBS surface families
  status: done
  depends_on:
  - AICAD-113
  - AICAD-110
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Bezier/B-spline/NURBS surfaces expose a bounded kernel-neutral construction/evaluation model with explicit U/V degrees,
    knots/multiplicities, control net, weights, and parameter domains.
  - Invalid degrees/control nets/knot vectors/multiplicities/weights/non-finite data fail deterministically; periodic/rational
    behavior is represented only where explicitly supported.
  - Known planar/bilinear/low-degree cases and sampled independent reference checks verify evaluation/derivatives within the
    approved approximation policy.
  - Public/HIR/IR data contains no native surface pointer or OCCT-specific class and is deterministic under repeated construction.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge
  - cargo test --workspace
  report: project/reports/AICAD-114.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-115
  stage: 5
  title: Implement trimmed-surface semantic model and bounded construction
  status: done
  depends_on:
  - AICAD-114
  - AICAD-111
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-2
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Trimmed surfaces explicitly represent an underlying surface plus bounded trimming loops/curves and orientation/domain
    semantics; trim identity is AICAD-owned and kernel-neutral.
  - Construction validates loop closure/orientation/domain consistency and reports invalid/self-inconsistent trim data rather
    than accepting silent kernel repair as semantic truth.
  - Tests cover holes/multiple loops where supported, reversed orientation, near-boundary cases, transformed surfaces, and
    exact/independent topology/property checks.
  - Any healing used during trim construction is explicit in operation evidence and uses the modeling/construction tolerance
    domain.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge
  - cargo test --workspace
  report: project/reports/AICAD-115.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-116
  stage: 5
  title: Implement surface evaluation, derivatives, normals, and offsets
  status: done
  depends_on:
  - AICAD-113
  - AICAD-114
  - AICAD-115
  - AICAD-106
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Surface evaluation/derivative/normal APIs and bounded offset operations run through ordinary typed calls with explicit
    parameter-domain and tolerance semantics.
  - Singularities, invalid offsets, self-intersection/degeneracy when detectable, and kernel failures produce structured reports/diagnostics
    rather than fabricated values.
  - Offset/approximation work reports the policy/evidence used and never borrows D5 equivalence tolerance or silently widens
    modeling tolerance.
  - Analytic and freeform fixtures verify derivatives/normals/offset distances with independent checks and deterministic repeated
    results.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge -p cad-query
  - cargo test --workspace
  report: project/reports/AICAD-116.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-117
  stage: 5
  title: Implement multi-solution geometry intersections, projections, and distance queries
  status: done
  depends_on:
  - AICAD-111
  - AICAD-116
  - AICAD-105
  - AICAD-106
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-8
  acceptance:
  - Approved curve/curve, curve/surface, and surface/surface intersection plus projection/closest/distance families execute
    as ordinary tracked kernel-backed queries and return explicit zero/one/many result structures.
  - Result cardinality and ambiguity are semantic, inspectable, and deterministic; coincident/overlapping/infinite-family
    or unsupported cases have explicit status rather than an arbitrary representative masquerading as a unique answer.
  - Ordering of finite result collections is defined by AICAD-owned deterministic keys/evidence, not kernel enumeration order;
    tolerances are operation-domain specific and explicit.
  - Source-level tests cover unique, zero, multiple, tangent/near-tangent, coincident/overlap, invalid-domain, and transformed
    cases with independent geometric checks.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-query -p cad-geometry-api -p cad-geometry-runtime -p cad-kernel-api -p cad-occt-bridge -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-117.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - a query family can only expose an arbitrary kernel-selected solution as unique
- id: AICAD-118
  stage: 5
  title: 'Checkpoint B: surfaces and multi-solution geometry-query gate'
  status: done
  depends_on:
  - AICAD-117
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-8
  acceptance:
  - Source-level vertical slices construct analytic/freeform/trimmed surfaces and exercise intersection/projection/distance
    through the real tracked runtime/kernel-neutral path with exact/independent verification.
  - A dedicated ambiguity/cardinality campaign proves no multi-solution operation silently selects one result and no query
    bypasses dependency/resource/provenance accounting.
  - At least one ACTIVE teaching example and one realistic surface/query example execute automatically; examples use actual
    supported .aicad syntax and current public APIs.
  - Full workspace/native verification passes and the report records explicit known numerical/coverage limits before topology
    work proceeds.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - native OCCT bridge CMake build plus CTest
  - run the automated ACTIVE example suite and record exact commands/results
  report: project/reports/AICAD-118.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-119
  stage: 5
  title: Implement general topology construction and exact validity evidence
  status: done
  depends_on:
  - AICAD-118
  - AICAD-115
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - Kernel-neutral topology construction covers the bounded Stage-5 vertex/edge/wire/face/shell/solid creation paths from
    approved curves/surfaces with explicit orientation/connectivity semantics.
  - Construction returns validation/operation evidence and never treats a successful native call or render as proof of a valid
    exact B-rep.
  - Invalid/open/non-manifold/orientation-inconsistent inputs are distinguished structurally and do not become safe semantic
    solids by accident.
  - Exact topology/property/import-round-trip checks verify representative constructions and preserve source/provenance context
    for lineage.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-kernel-api -p cad-occt-bridge -p cad-geometry-runtime -p cad-validation -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-119.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-120
  stage: 5
  title: Implement topology sewing and healing with explicit bounded policy
  status: done
  depends_on:
  - AICAD-119
  - AICAD-106
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-24
  acceptance:
  - Sewing/healing operations use explicit modeling/construction tolerance policy and return a structured report of requested
    work, changes, residual defects, validity, and any topology-changing consequences.
  - Healing is never an invisible fallback that changes geometry after an operation fails; safe outputs are produced only
    when post-operation validity/adoption requirements are satisfied.
  - Tests cover successful sewing, gaps inside/outside tolerance, orientation issues, non-manifold/invalid inputs, and deterministic
    repeated behavior.
  - Every topology-changing sewing/healing path exposes lineage/change evidence sufficient for AICAD-125 rather than discarding
    entity ancestry.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-kernel-api -p cad-occt-bridge -p cad-geometry-runtime -p cad-validation
  - cargo test --workspace
  report: project/reports/AICAD-120.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-121
  stage: 5
  title: Implement deterministic topology traversal and inspection
  status: done
  depends_on:
  - AICAD-119
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-25
  acceptance:
  - Safe topology inspection exposes kernel-neutral entity kinds, adjacency/incidence, orientation, geometry classification,
    properties, and traversal needed by Stage-5 algorithms without exposing native handles as semantic identity.
  - Observable traversal ordering is deterministic where an ordered result is promised; otherwise the API makes unordered/set
    semantics explicit and tests do not rely on kernel enumeration order.
  - Inspection can feed ordinary query/control-flow code through D23 without creating an untracked native side channel.
  - Fixtures cover shells/solids with multiple faces/edges, holes, non-manifold/open cases where representable, and transformed
    topology.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-kernel-api -p cad-occt-bridge -p cad-query -p cad-runtime
  - cargo test --workspace
  report: project/reports/AICAD-121.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-122
  stage: 5
  title: Implement the controlled raw/unsafe geometry tier with epoch-bound handles
  status: done
  depends_on:
  - AICAD-121
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-24
  - rfcs/0002-geometry-runtime-kernel-abstraction.md
  - project/reports/AICAD-093.md
  acceptance:
  - Raw/unsafe geometry values are opaque, AICAD-owned, kernel-neutral in public semantics, and bound to an explicit owning
    context/epoch; stale/wrong-context handles fail deterministically.
  - Raw handles are never serialized or reused as durable semantic topology identity, persistent references, feature identity,
    cache keys, or cross-build equality.
  - The source/API boundary makes entry into the raw tier explicit and auditable without exposing OCCT classes/pointers or
    creating an arbitrary native/plugin callback facility.
  - Adversarial tests cover stale epochs, wrong contexts, dropped/rebuilt owners, handle reuse attempts, and attempts to pass
    raw values where safe/persistent identity is required.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-references -p cad-kernel-api -p cad-occt-bridge -p cad-runtime -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-122.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-123
  stage: 5
  title: Implement functional raw topology editing with lineage/change evidence
  status: done
  depends_on:
  - AICAD-120
  - AICAD-122
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-2
  - project/DECISION_LOG.md#DL-24
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  acceptance:
  - 'The bounded Stage-5 raw editing surface follows D2 functional/value semantics: operations produce a new raw result/version
    rather than source-visible in-place mutation/aliasing semantics.'
  - Topology-changing edits return structured change/lineage evidence, validation state, diagnostics, and epoch/lifetime consequences;
    native mutation remains an implementation detail only.
  - Unsupported/invalid edits fail explicitly and cannot silently coerce a raw result into safe semantic geometry.
  - Tests cover entity deletion/replacement/split/merge edits, stale pre-edit handles, repeated deterministic edits, and preservation
    of AICAD-owned provenance.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-kernel-api -p cad-occt-bridge -p cad-geometry-runtime -p cad-references
  - cargo test --workspace
  report: project/reports/AICAD-123.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-124
  stage: 5
  title: Implement explicit raw-to-safe validation and adoption
  status: done
  depends_on:
  - AICAD-123
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-5
  - docs/plan/05_LOW_LEVEL_GEOMETRY_TOPOLOGY_API.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  acceptance:
  - Promotion from raw/unsafe geometry to normal safe semantic geometry exists only through explicit validation/adoption that
    produces a new safe value plus inspectable validation/provenance evidence.
  - Adoption rejects invalid, stale, wrong-context, or policy-violating raw inputs and never converts the raw handle itself
    into safe/persistent identity.
  - Adopted values re-enter ordinary feature/provenance/reference machinery with new AICAD-owned semantic identity and deterministic
    source/operation attribution.
  - Tests prove valid adoption, each rejection class, post-adoption independence from the raw handle lifetime, and exact B-rep
    validity/property evidence.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-validation -p cad-kernel-api -p cad-occt-bridge -p cad-feature-graph -p cad-references -p cad-cli
  - cargo test --workspace
  report: project/reports/AICAD-124.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-125
  stage: 5
  title: Integrate lineage and Stage-4 persistent-reference integrity across every Stage-5 topology change
  status: done
  depends_on:
  - AICAD-120
  - AICAD-123
  - AICAD-124
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md
  - project/DECISION_LOG.md#DL-8
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-27
  - project/gates/stage-4-gate.md
  acceptance:
  - Every topology-changing Stage-5 operation implemented through AICAD-124 emits/propagates lineage or an explicit insufficiency
    reason for affected supported entity kinds; no operation silently drops change evidence.
  - Stage-4 persistent references replay correctly across representative Stage-5 edits using the same fail-closed Resolved/Ambiguous/Broken
    contract, deliberate scopes, durability classes, and disabled fingerprint auto-recovery.
  - Adversarial split/merge/delete/recreate/coincident-geometry/reorder/raw-edit/adopt cases prove no native handle or geometric
    coincidence becomes persistent identity and every discovered silent-wrong result becomes a minimized permanent regression.
  - Feature/provenance identity from D25 remains traceable through curve/surface/topology abstractions and adopted raw outputs.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test -p cad-references -p cad-query -p cad-feature-graph -p cad-cli
  - python3 scripts/ci/semantic_ref_harness.py validate
  - python3 scripts/ci/semantic_ref_harness.py self-test
  - cargo test --workspace
  report: project/reports/AICAD-125.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - any Stage-5 topology change cannot provide lineage and would therefore weaken Stage-4 reference safety
- id: AICAD-126
  stage: 5
  title: 'Checkpoint C: topology, raw tier, adoption, lineage, and reference-integrity gate'
  status: done
  depends_on:
  - AICAD-125
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-8
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-27
  - project/gates/stage-4-gate.md
  acceptance:
  - A source/runtime vertical slice constructs, sews/heals, inspects, raw-edits, explicitly adopts, and persistently references
    Stage-5 topology while preserving safe/raw/reference identity separation.
  - The semantic-reference adversarial campaign reports zero surviving SILENT_WRONG outcomes; split/merge/edit cases are permanent
    regressions and fingerprint automatic recovery remains disabled.
  - Kernel-neutrality and raw-handle epoch tests prove no OCCT/native identity leaks above the adapter or becomes durable
    identity.
  - At least one ACTIVE teaching example and one realistic topology/raw-adoption example execute automatically; full workspace/native
    verification passes before hardening work proceeds.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - python3 scripts/ci/semantic_ref_harness.py validate
  - python3 scripts/ci/semantic_ref_harness.py self-test
  - native OCCT bridge CMake build plus CTest
  - run the automated ACTIVE example suite and record exact commands/results
  report: project/reports/AICAD-126.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-127
  stage: 5
  title: Build and freeze a difficult freeform exact-geometry corpus
  status: done
  depends_on:
  - AICAD-126
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/planning/roadmap/post100/STAGE5_TASK_DRAFT.yaml
  acceptance:
  - A versioned Stage-5 corpus exercises difficult but realistic freeform/exact geometry, including representative spline/NURBS
    hooks or bends, variable-section sweep/twisted loft, blade/impeller-like geometry, trimmed surfaces/open shells where
    applicable, and other nontrivial cases justified by the supported API.
  - Each fixture declares exact validity/topology/property/reference expectations and the tolerance domain used; render appearance
    is never its acceptance oracle.
  - The corpus separates tuning/public fixtures from any held-out/adversarial subset used as a checkpoint and records checksums/ground
    truth where appropriate.
  - Fixtures are executable through current .aicad/public APIs or explicitly classified as lower-level stress fixtures; no
    aspirational syntax is presented as current usage.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - run the Stage-5 corpus validation harness/fixtures and record exact commands/results
  report: project/reports/AICAD-127.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-128
  stage: 5
  title: Run Stage-5 numerical, robustness, determinism, and resource adversarial campaign
  status: done
  depends_on:
  - AICAD-127
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-12
  - project/DECISION_LOG.md#DL-26
  acceptance:
  - The difficult corpus plus targeted generated/adversarial cases exercise near-degenerate curves/surfaces, trim boundaries,
    tangent/overlap queries, sewing/healing limits, topology edits, and repeated rebuilds across relevant scale ranges.
  - Failures are classified into invalid input, explicit ambiguity/no-solution, kernel failure, numerical/robustness defect,
    reference defect, resource-limit failure, and unrelated failure; expected behavior is never weakened to make a case pass.
  - Every reproducible correctness/reference crash/hang/silent-wrong defect is minimized and preserved as a permanent regression;
    tolerance policies are not silently widened.
  - Performance/resource measurements are collected only where useful to expose pathological scaling or budget violations;
    no unsupported benchmark target is invented, and D5 deterministic-equivalence evidence is recorded separately from modeling
    tolerance.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - run the Stage-5 adversarial/freeform corpus campaign and record exact commands/results
  - rerun deterministic/repeat-build checks for minimized reproducers
  report: project/reports/AICAD-128.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
- id: AICAD-129
  stage: 5
  title: Prove core-vs-library boundary, learnability/inspectability, and maintained examples
  status: done
  depends_on:
  - AICAD-127
  - AICAD-128
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/00_PRINCIPLES_AND_SCOPE.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/DECISION_LOG.md#DL-23
  - project/DECISION_LOG.md#DL-27
  - AGENTS.md
  acceptance:
  - A Stage-5 surface audit proves geometry capabilities use ordinary language/RuntimeBuiltin/library mechanisms where appropriate
    and that no convenience-only compiler intrinsic, open native-registration boundary, or OCCT-specific public API has crept
    in.
  - Representative source can be inspected from source -> typed call/value -> feature/provenance/dependency -> Geometry IR/query
    -> kernel result/reference evidence with stable structured diagnostics, supporting human and tool/AI reasoning without
    private implementation knowledge.
  - ACTIVE examples are classified as teaching or realistic, execute automatically, cover each major Stage-5 checkpoint capability,
    and use actual current .aicad syntax; stale historical examples are archived rather than left as supported usage.
  - A bounded fresh-tool/client or AI-assisted fixture may measure whether structured public surfaces are usable, but compiler/runtime
    validation remains authoritative and no Stage-10 AI architecture is pulled forward.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - run the automated ACTIVE example suite and record exact commands/results
  - run the bounded inspectability/tool-client fixture and record exact commands/results
  report: project/reports/AICAD-129.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - the audit finds a Stage-5 capability only reachable through a compiler-intrinsic or kernel-specific shortcut
- id: AICAD-130
  stage: 5
  title: Prepare the final Stage-5 owner gate packet
  status: done
  depends_on:
  - AICAD-129
  plan_references:
  - project/planning/transitions/stage4-to-stage5/STAGE5_FINAL_BATCHES.md
  - docs/plan/16_TESTING_BENCHMARKS_ACCEPTANCE.md
  - project/gates/stage-4-gate.md
  - project/DECISION_LOG.md#DL-12
  - project/DECISION_LOG.md#DL-5
  - project/DECISION_LOG.md#DL-8
  - project/DECISION_LOG.md#DL-23
  - project/DECISION_LOG.md#DL-24
  - project/DECISION_LOG.md#DL-25
  - project/DECISION_LOG.md#DL-26
  - project/DECISION_LOG.md#DL-27
  - project/planning/transitions/stage4-to-stage5/STAGE_PROMOTION_POLICY.md
  acceptance:
  - The gate independently re-audits current implementation and re-runs required evidence rather than merely citing task-report
    claims; it covers every AICAD-101..129 acceptance item and the Stage-4 carry-forward prelude.
  - The packet explicitly proves D21-D25 compatibility, D7 fail-closed references with zero surviving silent-wrong regressions,
    kernel-neutral public semantics, separate tolerance domains, raw-handle epoch/non-identity rules, lineage for topology
    changes, realistic exact-geometry evidence, and maintained executable examples.
  - The packet records the difficult/freeform and adversarial campaign results, known limitations, performance/resource evidence
    where justified, unresolved owner decisions, and any evidence-sensitive assumptions relevant to provisional Stage 6.
  - Exactly one advisory PASS / PASS WITH CONDITIONS / DO NOT PASS recommendation is produced; the agent does not approve
    Stage 5, promote Stage 6, assign Stage-6 global AICAD IDs, or begin Stage-6 implementation.
  required_checks:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - python3 scripts/ci/semantic_ref_harness.py validate
  - python3 scripts/ci/semantic_ref_harness.py self-test
  - native OCCT bridge CMake build plus CTest
  - run the Stage-5 corpus/adversarial campaign
  - run the automated ACTIVE example suite and record exact commands/results
  report: project/reports/AICAD-130.md
  escalate_if:
  - public syntax/semantics must change beyond an approved RFC or resolved owner decision
  - a stage gate/benchmark/test would need weakening
  - kernel-specific types or native identity would leak above the kernel adapter
  - semantic-reference ambiguity would require arbitrary fallback or fingerprint auto-recovery
  - an unresolved architecture alternative must be selected
  - work would expand into Stage 6 or later roadmap scope
  - the Stage-5 gate would need to waive a failed invariant or conceal a known limitation
  - Stage-5 evidence materially invalidates a provisional Stage-6 architecture/semantic assumption; record it for owner reconciliation
    instead of silently promoting Stage 6
- id: AICAD-131
  stage: 6
  status: done
  batch: S6-00
  title: Stabilize Stage-5 compatibility and close prerequisite geometry/dependency gaps
  depends_on: []
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Freeze the Stage-5 spatial, query, reference, lineage, exact-geometry, and incremental-regeneration contracts Stage 6 relies on, and close the two known Stage-5 prerequisite gaps before assembly implementation begins, without reopening D26-D30.
  acceptance:
    - Compatibility fixtures cover nested source identity, stable semantic references, pose-safe geometry subjects, lineage/adoption evidence, exact-geometry behavior, and incremental regeneration before Stage-6 assembly semantics are layered on top.
    - Bezier/B-spline curves and surfaces, plus trimmed surfaces, can be converted into real kernel topology through `make_edge` / `make_face_on_surface` for supported mathematically/topologically valid inputs; supported cases no longer return `UNSUPPORTED_TOPOLOGY_CONSTRUCTION`, while invalid inputs fail deterministically with structured diagnostics.
    - `List<Geometry>` builtin parameters participate in `geometry_inputs` for both `FeatureGraph` and `TraceFeatureGraph`, and generic-list geometry dependencies drive correct dirty-set / incremental invalidation rather than remaining invisible.
    - Focused regression fixtures prove the new freeform-to-topology and `List<Geometry>` dependency behavior without weakening existing Stage-4/5 fail-closed semantic-reference, lineage, exactness, or determinism guarantees.
    - AICAD-132 and later Stage-6 work may rely on these two repaired prerequisite paths only after the AICAD-131 evidence and required tests pass.
  required_checks: [focused Stage-5 compatibility fixtures, freeform-to-kernel-topology construction and invalid-input diagnostics, List<Geometry> geometry_inputs and dirty-set invalidation fixtures for FeatureGraph and TraceFeatureGraph, existing semantic-reference harness, existing active example suite]
  report: project/reports/AICAD-131.md
  escalate_if: [actual Stage-5 behavior contradicts D26-D30 or prevents cross-instance semantic references, either prerequisite fix requires new public semantics beyond approved Stage-5 contracts, either fix cannot be completed without weakening an existing invariant or gate]

- id: AICAD-132
  stage: 6
  status: done
  batch: S6-01
  title: Implement general interfaces/protocols and bounded generics
  depends_on: [AICAD-131]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Provide the D27 language machinery used by assemblies without assembly-only compiler magic.
  acceptance: [interfaces/conformance and bounded generics are ordinary typed language mechanisms, mechanical-interface use requires no named compiler special case, diagnostics are deterministic and source-traceable]
  required_checks: [positive and negative conformance fixtures, bounded-generic type tests, deterministic diagnostics]
  report: project/reports/AICAD-132.md
  escalate_if: [D27 cannot express required mechanical-interface conformance without new public semantics]

- id: AICAD-133
  stage: 6
  status: done
  batch: S6-01
  title: Implement assembly semantic identity primitives
  depends_on: [AICAD-131]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Establish the distinct D26 assembly identity domains before assembly realization exists.
  acceptance: [definition identity logical-instance identity nested-occurrence/path identity configuration-slot identity external-asset identity semantic-topology identity and BOM identity remain distinct, identities are deterministic reproducible and type-safe, no identity derives from topology indices solver variables filesystem paths or BOM rows]
  required_checks: [identity-domain type tests, deterministic serialization/rebuild tests, collision/adversarial fixtures]
  report: project/reports/AICAD-133.md
  escalate_if: [any required identity domain would have to collapse or alias another D26 domain]

- id: AICAD-134
  stage: 6
  status: done
  batch: S6-02
  title: Implement component-definition and logical-instance semantic IR
  depends_on: [AICAD-132, AICAD-133]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Represent reusable component definitions separately from logical instances and realized geometry.
  acceptance: [definitions and instances are distinct semantic entities, shared definitions stay shared under reuse and parameterization, realized geometry occurrence is never authoritative assembly identity]
  required_checks: [definition reuse fixtures, repeated-instance identity tests, parameterized definition rebuild tests]
  report: project/reports/AICAD-134.md
  escalate_if: [ordinary language abstraction would erase feature identity dependencies provenance incremental behavior or reference support]

- id: AICAD-135
  stage: 6
  status: done
  batch: S6-02
  title: Implement local/world frames and rigid instance poses
  depends_on: [AICAD-134]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Reuse Stage-5 spatial semantics for deterministic instance pose composition and inversion.
  acceptance: [local/world pose composition is typed and deterministic, pose changes preserve logical identity and semantic-reference identity, frame conventions are explicit and kernel-neutral]
  required_checks: [composition/inverse round trips, nested-frame fixtures, pose-only reference-durability tests]
  report: project/reports/AICAD-135.md
  escalate_if: [Stage-5 frame semantics cannot support deterministic nested instance poses]

- id: AICAD-136
  stage: 6
  status: done
  batch: S6-02
  title: Implement nested-assembly dependency graph and cycle diagnostics
  depends_on: [AICAD-134, AICAD-135]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Model assembly nesting/dependencies separately from solver relation graphs.
  acceptance: [nested definitions and instances resolve deterministically, shared definitions remain shared, illegal cycles fail with source-traceable diagnostics, assembly dependency graph is not conflated with numerical solver graph]
  required_checks: [nested/reused definition fixtures, cycle diagnostics, deterministic graph-order tests]
  report: project/reports/AICAD-136.md
  escalate_if: [nesting semantics require a new public identity rule]

- id: AICAD-137
  stage: 6
  status: done
  batch: S6-03
  title: Implement assembly-level semantic-reference addressing
  depends_on: [AICAD-133, AICAD-136]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Compose logical instance paths with Stage-4/5 semantic geometry recipes for cross-instance references.
  acceptance: [pose-only changes preserve references, cross-instance ambiguity fails closed, definition/configuration changes follow explicit durability rules, no topology index or native handle becomes semantic identity]
  required_checks: [cross-instance resolved ambiguous broken fixtures, pose perturbation corpus, split/merge/edit lineage fixtures]
  report: project/reports/AICAD-137.md
  escalate_if: [Stage-5 geometry/reference behavior prevents fail-closed cross-instance semantic references]

- id: AICAD-138
  stage: 6
  status: done
  batch: S6-03
  title: Implement reusable mechanical-interface semantics
  depends_on: [AICAD-132, AICAD-137]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Build mechanical interfaces on D27 general nominal interfaces/protocols plus typed frames references and parameters.
  acceptance: [mechanical interfaces are ordinary reusable semantic values/conformance contracts, static conformance and geometry-dependent compatibility remain distinguishable, no assembly-only compiler intrinsic is introduced]
  required_checks: [interface conformance fixtures, typed frame/reference compatibility tests, incompatible-interface diagnostics]
  report: project/reports/AICAD-138.md
  escalate_if: [mechanical interfaces require compiler magic outside D27]

- id: AICAD-139
  stage: 6
  status: done
  batch: S6-04
  title: Implement solver-neutral mate relation IR
  depends_on: [AICAD-137, AICAD-138]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Represent mates as AICAD-owned typed semantic relations over stable assembly subjects.
  acceptance: [mate identity subjects and semantics are inspectable without a solver, solver IDs weights and native expressions are absent from public semantics, source/provenance evidence is stable]
  required_checks: [mate IR round trips, relation identity tests, solver-absence inspection tests]
  report: project/reports/AICAD-139.md
  escalate_if: [a solver backend would define public mate meaning]

- id: AICAD-140
  stage: 6
  status: done
  batch: S6-04
  title: Implement solver-neutral joint and coordinate IR
  depends_on: [AICAD-139]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Represent approved joint families and coordinates independently of backend parameterization.
  acceptance: [joint families and coordinates are typed dimensionally correct and inspectable without a solver, limits and coordinate conventions are explicit, backend parameterization is non-authoritative]
  required_checks: [joint-family type fixtures, coordinate/limit validation, solver-absence inspection tests]
  report: project/reports/AICAD-140.md
  escalate_if: [joint semantics cannot be stated independently of a numerical backend]

- id: AICAD-141
  stage: 6
  status: done
  batch: S6-04
  title: Checkpoint A - semantic assembly graph interfaces references mates and joints
  depends_on: [AICAD-140]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Prove the semantic assembly model works without any numerical solver.
  acceptance: [a nested multi-instance fixture resolves definitions paths frames semantic references interfaces mates and joints with no numerical solver present, serialized observation and identities are deterministic, no topology-index or solver-native identity leaks]
  required_checks: [Checkpoint-A canonical fixture, clean rebuild determinism, semantic-reference perturbation suite]
  report: project/reports/AICAD-141.md
  escalate_if: [Checkpoint A requires weakening D26 D27 or D28]

- id: AICAD-142
  stage: 6
  status: done
  batch: S6-05
  title: Define numerical assembly-solver adapter contract
  depends_on: [AICAD-141]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Lower AICAD semantic relation graphs to replaceable numerical backends and map results back deterministically.
  acceptance: [adapter contract accepts semantic-neutral numeric inputs and returns poses residuals convergence and diagnostics, mock/alternate adapters are conformance-testable, backend objects never become public semantic identifiers]
  required_checks: [mock adapter conformance, alternate-adapter mapping fixture, structured failure tests]
  report: project/reports/AICAD-142.md
  escalate_if: [replaceable adapter boundary cannot preserve AICAD-owned semantics]

- id: AICAD-143
  stage: 6
  status: done
  batch: S6-05
  title: Implement deterministic grounding initial-pose and representative-pose semantics
  depends_on: [AICAD-142]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Make gauge/root and underconstrained representative-pose policy explicit before relying on a solver.
  acceptance: [grounding/root policy is explicit, underconstrained assemblies report remaining DOF, relation insertion order and initial guesses do not arbitrarily change source-visible pose semantics]
  required_checks: [gauge-equivalence fixtures, relation-order permutations, initial-guess perturbations, underconstraint fixtures]
  report: project/reports/AICAD-143.md
  escalate_if: [deterministic representative-pose policy is insufficient for an approved relation family]

- id: AICAD-144
  stage: 6
  status: todo
  batch: S6-05
  title: Implement baseline assembly numerical solver
  depends_on: [AICAD-142, AICAD-143]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Provide one bounded deterministic numerical backend behind the solver-neutral adapter.
  acceptance: [baseline relation set solves through the adapter, convergence/residual evidence is structured, failure never leaves partial authoritative poses, backend implementation does not redefine public relation semantics]
  required_checks: [baseline relation solve corpus, deterministic repeated solves, nonconvergence/failure fixtures]
  report: project/reports/AICAD-144.md
  escalate_if: [numerical behavior would need to define public mate/joint semantics]

- id: AICAD-145
  stage: 6
  status: todo
  batch: S6-06
  title: Implement degrees-of-freedom analysis
  depends_on: [AICAD-142, AICAD-144]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Report remaining semantic DOF with explanatory assembly evidence.
  acceptance: [known mechanisms report expected DOF, results identify relevant semantic subjects/relations, numerical rank thresholds are explicit solver-policy evidence rather than identity]
  required_checks: [free rigid body hinge slider fixed and nested mechanism DOF fixtures, threshold-boundary tests]
  report: project/reports/AICAD-145.md
  escalate_if: [DOF semantics cannot be separated from backend-specific variables]

- id: AICAD-146
  stage: 6
  status: todo
  batch: S6-06
  title: Implement redundancy and conflict diagnostics
  depends_on: [AICAD-144, AICAD-145]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Distinguish inconsistent and redundant assembly relations using stable semantic evidence.
  acceptance: [conflict redundancy and ordinary underconstraint are structurally distinct, diagnostics reference stable relations instances and semantic refs, native solver variables are not the user contract]
  required_checks: [redundant/contradictory relation fixtures, minimal explanatory evidence tests, deterministic diagnostic ordering]
  report: project/reports/AICAD-146.md
  escalate_if: [structured conflict/redundancy cannot be expressed without exposing backend variables]

- id: AICAD-147
  stage: 6
  status: todo
  batch: S6-06
  title: Implement baseline kinematic evaluation
  depends_on: [AICAD-140, AICAD-144, AICAD-145]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Evaluate approved mechanisms over typed bounded joint coordinates while preserving assembly identity.
  acceptance: [joint-coordinate evaluation respects typed limits and deterministic poses, instance/reference identity is preserved through motion, dynamics and time integration remain out of scope]
  required_checks: [hinge slider and nested mechanism sweeps, coordinate-limit fixtures, reference durability through motion]
  report: project/reports/AICAD-147.md
  escalate_if: [kinematic evaluation requires unapproved dynamics or mutable identity semantics]

- id: AICAD-148
  stage: 6
  status: todo
  batch: S6-06
  title: Checkpoint B - solver-neutral pose DOF conflict and kinematic realization
  depends_on: [AICAD-146, AICAD-147]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Prove deterministic solver-neutral realization across canonical mechanisms.
  acceptance: [hinge slider and nested mechanism fixtures pass pose DOF conflict and motion checks, adapter replacement preserves public meaning, relation-order perturbation yields equivalent normalized results]
  required_checks: [Checkpoint-B corpus, adapter substitution test, relation-order and initial-pose perturbation campaign]
  report: project/reports/AICAD-148.md
  escalate_if: [Checkpoint B requires backend-specific semantics or nondeterministic observable pose]

- id: AICAD-149
  stage: 6
  status: todo
  batch: S6-07
  title: Implement immutable configuration overlays
  depends_on: [AICAD-133, AICAD-148]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Implement D29 configurations as immutable semantic overlays over stable authoritative model state.
  acceptance: [configuration identity and resolution are deterministic, base model state is not destructively mutated, overlays preserve provenance and logical identity]
  required_checks: [overlay immutability tests, deterministic resolution/rebuild, base-state preservation fixtures]
  report: project/reports/AICAD-149.md
  escalate_if: [configuration semantics require destructive mutation or identity loss]

- id: AICAD-150
  stage: 6
  status: todo
  batch: S6-07
  title: Implement configuration validity rules
  depends_on: [AICAD-149]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Evaluate typed declarative configuration rules without a bespoke hidden rule language.
  acceptance: [rules evaluate deterministically with source evidence, dependency cycles are diagnosed where applicable, ordinary expressions are reused when sufficient]
  required_checks: [valid/invalid rule fixtures, cycle diagnostics, deterministic evaluation order]
  report: project/reports/AICAD-150.md
  escalate_if: [required configuration policy cannot be represented by D29 plus existing language semantics]

- id: AICAD-151
  stage: 6
  status: todo
  batch: S6-07
  title: Implement suppression with stable logical identity
  depends_on: [AICAD-149, AICAD-150]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Make suppression an inactive overlay state rather than deletion.
  acceptance: [suppressed subjects remain traceable and identities are not recycled, references mates and BOM behavior across suppression are deterministic and explicit, reactivation restores the same logical subject]
  required_checks: [suppress/reactivate identity fixtures, reference/mate behavior tests, BOM suppression tests]
  report: project/reports/AICAD-151.md
  escalate_if: [suppression would require destructive deletion or identity recycling]

- id: AICAD-152
  stage: 6
  status: todo
  batch: S6-08
  title: Implement replacement and variant selection
  depends_on: [AICAD-132, AICAD-138, AICAD-149, AICAD-151]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Bind stable logical slots to compatible concrete definitions while keeping logical and concrete identities distinct.
  acceptance: [replacement uses D27 conformance, unavailable semantic outputs fail closed, provenance references and BOM explicitly reflect selected variants without overwriting slot identity]
  required_checks: [compatible/incompatible replacement fixtures, missing-output fail-closed tests, identity/provenance/BOM variant tests]
  report: project/reports/AICAD-152.md
  escalate_if: [replacement cannot preserve logical identity/provenance or needs assembly-only conformance semantics]

- id: AICAD-153
  stage: 6
  status: todo
  batch: S6-08
  title: Implement external-asset component identity and provenance
  depends_on: [AICAD-133, AICAD-137]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Implement D30 stable external-asset identity independent of paths import indices and native handles.
  acceptance: [asset records include content digest importer/settings and provenance identity, changed content or import settings invalidate deterministically, filesystem path STEP entity number and native index are non-authoritative]
  required_checks: [same-content/different-path fixtures, changed-content invalidation, importer-setting identity tests, imported-reference tests]
  report: project/reports/AICAD-153.md
  escalate_if: [external assets require filesystem or kernel identity]

- id: AICAD-154
  stage: 6
  status: todo
  batch: S6-09
  title: Implement deterministic BOM semantics
  depends_on: [AICAD-134, AICAD-151, AICAD-152, AICAD-153]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Derive BOM structure from semantic definitions instances configurations and purchasing identity rather than geometry occurrences.
  acceptance: [nested quantities aggregate deterministically, suppression replacement and external components change BOM only through explicit semantic rules, purchasing identity remains distinct from assembly and topology identity]
  required_checks: [nested quantity fixtures, suppression/replacement BOM tests, deterministic ordering and identity tests]
  report: project/reports/AICAD-154.md
  escalate_if: [BOM generation would collapse D26 identity domains]

- id: AICAD-155
  stage: 6
  status: todo
  batch: S6-09
  title: Implement static interference/collision query baseline
  depends_on: [AICAD-137, AICAD-148, AICAD-151, AICAD-152]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Provide deterministic static interference results over semantic assembly subjects using explicit tolerance policy.
  acceptance: [results identify logical instances and semantic geometry subjects, approved contact states are distinguished under explicit tolerance policy, ordering is independent of native traversal/acceleration structure]
  required_checks: [separated/touching/interfering fixtures, tolerance-boundary tests, deterministic-order tests, configuration/replacement fixtures]
  report: project/reports/AICAD-155.md
  escalate_if: [Stage-5 query/tolerance contracts are insufficient for deterministic assembly interference]

- id: AICAD-156
  stage: 6
  status: todo
  batch: S6-09
  title: Checkpoint C - configurations external assets BOM and interference
  depends_on: [AICAD-150, AICAD-151, AICAD-152, AICAD-153, AICAD-154, AICAD-155]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Prove configuration and engineering-output semantics on one nested configurable assembly.
  acceptance: [fixture exercises suppression replacement imported component BOM and interference, logical references are preserved or explicitly broken according to documented rules, rebuild depends only on source configuration and locked asset identity]
  required_checks: [Checkpoint-C canonical fixture, clean rebuild determinism, configuration churn and locked-asset perturbations]
  report: project/reports/AICAD-156.md
  escalate_if: [Checkpoint C requires destructive configuration identity or path/native external-asset identity]

- id: AICAD-157
  stage: 6
  status: todo
  batch: S6-10
  title: Build canonical realistic assembly corpus
  depends_on: [AICAD-156]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Maintain realistic source-first assembly fixtures with machine-checked engineering expectations.
  acceptance: [hinge slider gearbox-or-actuator nested-or-robot-arm vendor-component and replacement/configuration fixtures are covered, expected pose DOF conflicts interference BOM and reference outcomes are machine checked, fixtures use current public source syntax]
  required_checks: [automated canonical assembly corpus, clean rebuild and deterministic-output checks]
  report: project/reports/AICAD-157.md
  escalate_if: [a realistic fixture exposes a semantic gap requiring a new owner decision]

- id: AICAD-158
  stage: 6
  status: todo
  batch: S6-11
  title: Run adversarial identity solver configuration and bounded-scale campaign
  depends_on: [AICAD-157]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Stress the semantic model against ambiguity singularity churn nesting and bounded scale without relaxing invariants.
  acceptance: [symmetry singularity overconstraint underconstraint nesting configuration churn imported geometry and larger bounded instance sets never cause arbitrary identity pose or reference reassignment, resource limits are measured and explicit, zero silent-wrong identity/reference outcomes are accepted]
  required_checks: [adversarial campaign, repeated-order/seed perturbations where applicable, bounded resource measurements]
  report: project/reports/AICAD-158.md
  escalate_if: [adversarial evidence exposes nondeterministic public semantics or requires invariant weakening]

- id: AICAD-159
  stage: 6
  status: todo
  batch: S6-11
  title: Expose structured assembly CLI/API inspection and maintain current examples
  depends_on: [AICAD-157, AICAD-158]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Make assembly semantics inspectable through stable structured tooling and maintained executable examples.
  acceptance: [versioned machine-stable output exposes definitions instances paths interfaces relations poses DOF conflicts configurations BOM and interference without private solver/native objects, active assembly examples run automatically, human help/docs use canonical terminology]
  required_checks: [structured-schema tests, CLI golden tests, active example suite, documentation command smoke]
  report: project/reports/AICAD-159.md
  escalate_if: [stable inspection requires exposing solver/native implementation identity]

- id: AICAD-160
  stage: 6
  status: todo
  batch: S6-12
  title: Prepare Stage-6 owner hard-gate packet
  depends_on: [AICAD-159]
  plan_references:
  - project/planning/transitions/stage5-to-stage6/STAGE6_FINAL.yaml
  - project/planning/transitions/stage5-to-stage6/TRANSITION_REPORT.md
  objective: Produce the final evidence packet and recommendation without promoting or implementing Stage 7.
  acceptance: [Checkpoints A B and C are complete, canonical and adversarial evidence demonstrates solver replaceability deterministic identity pose and configuration behavior structured tooling and current examples, all material limitations and resource observations are explicit, exactly one advisory PASS PASS-WITH-CONDITIONS or DO-NOT-PASS recommendation is recorded, no Stage-7 global IDs are assigned and no Stage-7 implementation begins]
  required_checks: [cargo fmt/clippy/workspace test, complete active example suite, semantic-reference and assembly adversarial campaigns, representative clean-user CLI/build checks]
  report: project/reports/AICAD-160.md
  escalate_if: [the Stage-6 gate would need to waive a failed invariant conceal a material limitation or invent a Stage-7 owner decision]
