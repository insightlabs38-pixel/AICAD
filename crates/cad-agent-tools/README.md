# cad-agent-tools

WP-16 (AI tools + skills). Structured tool server
(`cad.build/test/inspect/query/docs/search/explain/render/diff/export/packages`),
compact agent context packets, package-skill loading, learnability
benchmark harness, AI-specific lint rules. Must not bypass compiler
validation — AI support sits on top of the compiler/runtime, never inside
the geometry engine.

Plan references: `docs/plan/11_AI_NATIVE_SKILLS_AND_AGENT_PROTOCOL.md`;
`docs/plan/22_REPOSITORY_WORK_PACKAGES.md` WP-16.
