# cad-query

Query AST/IR for geometry/topology/spatial predicates and
ranking/disambiguation (paired with `cad-references`; part of WP-07).
Queries are criteria objects, not materialized lists, so regeneration can
re-evaluate them.

Plan references: `docs/plan/06_REFERENCES_QUERIES_FEATURE_DAG.md` §5-6;
`docs/plan/23_CROSS_SYSTEM_PARAMETER_CATALOG.md` §6.
