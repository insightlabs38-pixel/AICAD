# examples/plates

Example library category: mounting-plate style parts
(`docs/plan/18_REFERENCE_EXAMPLES.md` Example 1).

Plan references: `docs/plan/22_REPOSITORY_WORK_PACKAGES.md` §1.

- `stage3_bearing_mount.aicad` — a Stage-3 ordinary-part example
  (`AICAD-079`): a bearing-mount plate with a bossed central bore, a
  6-hole bolt circle (`radial_pattern`), and a corner keyway (`pocket`) —
  see `crates/cad-cli/tests/stage3_ordinary_parts.rs` for its build/
  validity proof.
