# examples/enclosures

Example library category: enclosure/housing parts, including the suggested
"motor/gearbox enclosure" first vertical demonstration.

Plan references: `docs/plan/20_REVIEW_PASS_GAPS_AND_DECISIONS.md` §6;
`docs/plan/22_REPOSITORY_WORK_PACKAGES.md` §22.

- `stage3_enclosure.aicad` — a Stage-3 ordinary-part example (`AICAD-079`):
  a thin-walled open-top housing (`shell`) with one cable-access hole
  (`hole`) — see `crates/cad-cli/tests/stage3_ordinary_parts.rs` for its
  build/validity proof.
